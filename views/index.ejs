<!DOCTYPE html>
<html>
<head>
    <title><%= pageTitle %></title>
    <style>
        .post {
            margin: 20px 0;
            padding: 15px;
            border: 1px solid #eee;
            border-radius: 5px;
        }
        .post h2 {
            color: #333;
            margin-top: 0;
        }
        .post-date {
            color: #666;
            font-size: 0.9em;
        }
    </style>
</head>
<body>
    <h1><%= pageTitle %></h1>
    
    <% posts.forEach(post => { %>
        <%- post.html %>  <!-- This inserts the pre-built HTML -->
    <% }); %>
</body>
</html>